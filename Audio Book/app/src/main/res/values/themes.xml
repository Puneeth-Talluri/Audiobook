<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SoundSaga" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
            <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:popupBackground">@color/navyblue</item>

        <item name="android:fontFamily">@font/rubik_regular</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SoundSaga" parent="Base.Theme.SoundSaga" />

    <style name="CustomPopupMenu" parent="Widget.AppCompat.PopupMenu.Overflow">
        <item name="android:itemBackground">@color/navyblue</item>
        <!-- You can also customize other attributes like text color -->
        <item name="android:textColor">@color/white</item>
    </style>

</resources>